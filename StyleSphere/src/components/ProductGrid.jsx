// src/components/ProductGrid.jsx

//import './ProductGrid.css';

//const products = [
  /*{ id: 1, name: 'Product 1', price: '$50', image: '/assets/product1.jpg' },
  { id: 2, name: 'Product 2', price: '$60', image: '/assets/product2.jpg' },
  { id: 3, name: 'Product 3', price: '$70', image: '/assets/product3.jpg' },

const ProductGrid = () => {
  return (
    <section className="product-grid">
      {products.map((product) => (
        <div key={product.id} className="product">
          <img src={product.image} alt={product.name} />
          <h2>{product.name}</h2>
          <p>{product.price}</p>
        </div>
      ))}
    </section>
  );
};

export default ProductGrid;
*/